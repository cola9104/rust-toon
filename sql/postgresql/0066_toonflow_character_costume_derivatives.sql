UPDATE toonflow.skill_list
SET content = content || $rule$

## 中性人物底模的服装衍生规则（最高优先级）

- 当前项目所有人物父资产均为中性底模：男性仅穿白色安全短裤，女性仅穿白色抹胸和白色安全短裤；父资产不代表角色在剧本中的正式服装。
- 分析当前剧本时，必须逐一遍历 `assets` 中所有 `type=role` 且在剧本出场的人物，不得只处理发生显式换装的人物。
- 每个出场人物至少创建一条与其身份、场景和剧本描述一致的正式服装衍生资产。若同一人物在剧本中存在多套稳定服装，则分别创建。
- 剧本明确出现的重伤包扎、变身、异化或其它整体形态变化，应在正式服装之外额外创建衍生资产。
- 已有 `derive` 只用于去重；若某人物只有形态衍生但没有正式服装衍生，仍必须补充正式服装。
- 返回结果前核对：当前剧本每个出场人物都至少有一条可用于分镜制作的服装衍生资产。任何人物遗漏都视为任务未完成。
$rule$,
    update_time = EXTRACT(EPOCH FROM NOW()) * 1000
WHERE path = 'production_execution_derive_assets.md';
