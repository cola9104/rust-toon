# Toonflow Agent 与内容链路对齐方案

审计日期：2026-09-11。

对照基线：官方 `HBAI-Ltd/Toonflow-app` 的 `v1.1.8`，commit `cd3e7c4e83963bea255be2e621eb78d2cd1c2188`。以下是源码静态审计，不代表已完成浏览器全流程验收或真实模型效果对比。Rust 侧包含当前工作区未提交修复；数据库里的用户自定义 Skill、手册、模型映射还需在实施第一步读取并核对，不能把迁移内的种子内容当作运行中的最终配置。

## 当前实施进度

2026-09-11 已开始 P0 第一批实现：

- 生产项目上下文已补齐项目名称、类型、简介、画风、导演手册、模型、画幅、模式和剧本信息，并统一“用户修改与项目/剧本事实优先，画风只约束审美”的冲突规则。
- 资产提取、资产润色和生图均已接入项目世界观；古风项目中的现代 404 公寓不会因画风而丢失电脑、冷白灯、冰雹、泡面桶等明确事实。
- 动态 Skill 已改为按具体生产阶段和项目当前选项精确筛选；`creative_manuals` 中的选中画风/导演手册可通过只读虚拟路径加载，不再因项目选了画风就放行整个画风目录。
- 内部工具请求携带具体阶段 Agent 身份，API 调用方不能伪造该字段扩大 Skill 范围；已移除仅凭路径前缀放行主 Skill 的旁路。
- `add_deriveAsset` 的说明与实际参数已统一包含 `appearanceId`；生产流水线规则已统一为规划、按需衍生、确认后生成、分镜/审核、面板与分镜图。
- 衍生资产提示词恢复人工编辑和刷新；人工保存内容继续作为后续生成输入。

2026-09-11 继续完成 P0 第二批：

- 新增只读运行库清单脚本 `script/audit-toonflow-alignment.py`，核对固定上游 commit 后输出内容哈希、Skill 归属、项目选择、Agent 模型与模型提示词映射。当前清单位于 `.codex/audit/toonflow-alignment/runtime-inventory.json`：15 个 Skill 中 7 个与基线一致、8 个不同；23 本手册共 168 个条目与基线一致；模型提示词映射为空。差异不能证明是人工修改，因此不自动覆盖。
- 生产决策、执行、修复和自动复审复用同一项目/剧本上下文，补齐导演手册与模型信息；跨项目或不存在的剧本会明确报错。
- 修复“分镜表 Agent 有技能目录但没有加载工具”的断点。所有生产执行入口都获得 `use_skill`/`read_skill_file`，目录由 `available_skills` 统一解析，包含显式分配的自定义 Skill；自动复审也能加载当前项目手册。只补只读技能工具，不扩大写工具范围。
- 修复衍生工具的 `id:null` schema 不一致，将图片生成工具说明改为实际的整批等待语义；加载技能的工具记录新增内容 SHA-256、具体 Agent 和项目ID。
- 图片调用在现有 `toonflow.tasks.input` 中记录 `promptProvenance`：项目世界观、画风、原始资产描述、已保存提示词、本次提示词、身份准备结果、模板键及内容、参考图顺序摘要、资产/图片ID。最终请求 `prompt` 与 `promptSha256` 在构图参考附加后记录，失败任务也保留；任务中心“原始输入”可直接查看，无需新增数据库字段。
- 修复旧场景描述覆盖当前手改提示词的问题：生图以本次提交/已保存的非空提示词为事实输入，旧提取描述作为来源快照保留，空提示词才回退原描述。原描述没有“积水”不再删除后来明确保存的“地板有积水”。

本批验证：

- `cargo test -p rust-toon-toon-server --lib`：153 项通过，14 项依赖专用环境的测试默认忽略。
- `bash script/test-toonflow-alignment.sh`：隔离 PostgreSQL、本地模拟模型，覆盖导演规划、分镜表、面板、监制的原生工具加载链路、Skill 越阶段访问拒绝，以及现代404公寓、古代客栈、古今同框三种实际图片请求。逐项核对手改文本、最终发送内容与任务记录；模拟图片接口故意返回失败，以验证失败任务仍可追溯，不生成媒体。
- `bash script/test-image-contract.sh`：图片契约回归通过，并核对人物构图参考附加后的实际供应商 prompt 与任务记录一致。
- `pnpm --dir apps/web exec vitest run apps/web-antd/src/views/toonflow/projects/ProductionAssetStrip.test.ts`：编辑保存测试通过；`pnpm --dir apps/web --filter @vben/web-antd run typecheck` 通过。
- `node script/test-toonflow-alignment-ui.mjs`：真实 Chromium、Vue 和 Ant Design 组件验证编辑 → 保存 → 刷新 → 重开 → 生成事件携带新 prompt。API 模块被模拟，不修改真实项目。脚本默认连接 `http://127.0.0.1:5668` 的 Vite，可通过 `ALIGNMENT_UI_URL` 指定地址；截图位于 `.codex/audit/toonflow-alignment/prompt-editor.png`。

2026-09-11 已开始 P1 第一批实现：

- 图片任务新增版本化 `input_hash`，由资产类型和人工 prompt、项目简介/画风/图片模型/质量、父资产及参考图、模型提示词映射、请求模型和尺寸共同计算。数据库唯一索引保证同一资产、同一输入最多只有一个进行中任务。
- 单张、批量和 Agent 衍生生成都会先复用同指纹的进行中任务或已完成且仍被选中的图片；响应返回 `imageId`、`state`、`filePath` 与 `reused`。Agent 提交的资产 ID 会排序去重，只等待本次解析出的唯一任务。
- 修改资产 prompt 会在事务内解除旧图并取消该资产进行中的旧指纹任务；修改项目简介、画风、图片模型或质量会解除项目内已有生成图并取消旧任务。上传图片和没有生成指纹的历史图片不会被项目配置修改误清除。
- 生产子 Agent 已接入父 WebSocket 消息流。导演规划、衍生分析/生成、分镜表、面板、分镜图和监制审核分别创建阶段消息，完成时回填产出，失败时标记具体阶段错误。
- 新增迁移 `0018_asset_image_generation_idempotency.sql`，迁移计数、空库断言和 `sql/bootstrap/current.sql` 已同步。

P1 第一批验证：

- `cargo test -p rust-toon-toon-server --lib`：153 项通过，14 项专用环境测试默认忽略。
- `bash script/test-toonflow-alignment.sh`：隔离数据库回归覆盖并发重复请求复用、资产 prompt 修改失效、项目生成配置修改失效，以及原有模型边界与 Skill 范围。
- `bash script/test-image-contract.sh`：图片契约及重复进行中任务复用通过。
- `bash script/test-database-migrations.sh`：18 个迁移从空库完整应用且幂等，全部数据库专项回归通过。

2026-09-11 已开始 P2 第一批实现：

- 项目工作台新增“穿越剧场”创建入口和模板选择，项目卡明确显示剧场模板；模板只设置 `projectType=time_travel` 和题材默认值，不虚构故事简介、年代、地点或人物。
- 修复 `project_type` 只存库但内容链路误读 `type` 的问题。剧本 Agent、生产 Agent、资产提取、资产润色、图片生成与生成指纹现在同时区分剧场模板、故事题材和项目简介。
- 穿越模板在服务端统一注入世界观约束：场次决定时空，古今同框逐元素保留年代，跨时代换装保持同一人物身份，画风不能统一或替换时代事实。标准剧场不注入该专题规则。
- 模板单元测试确认不会自动补写宫殿等故事事实；隔离模型边界回归已改用真实 `time_travel` 项目，确认约束抵达各生产阶段和图片供应商请求。
- `project-templates.test.ts` 2 项、前端类型检查、Rust 154 项单元测试和 `test-toonflow-alignment.sh` 隔离回归均通过。
- 新增视觉手册“通用真人影视基底”（`realpeople_cinematic_base`），从既有两套真人现代手册提取真人面部、自然肤质、材质、摄影机成像、曝光和色彩响应等共同基线，移除都市、服装、建筑及时代绑定。角色、场景、道具、分镜、视频和导演规划共 12 个条目统一采用“基础画风控制怎么拍，项目与镜头事实控制拍什么”的规则。
- 新手册通过追加迁移 `0019_generic_live_action_visual_base.sql` 安装，冲突时保留同路径已有手工内容；场景画风回归确认现代公寓和唐代客栈都不会触发时代替换。
- 内置“风格库”与视觉手册原本由同步脚本写入两份近似数据。追加迁移 `0020_merge_builtin_style_library_into_visual_manuals.sql` 后，菜单统一进入完整创作手册，删除 11 条可确认的内置 `art_styles` 副本并保留时间戳 ID 的用户自建风格；项目和 Agent 继续以 `creative_manuals` 为唯一内置画风来源。空库 20 个迁移及 0019/0020 幂等性均已验证。

当前只新增了上述独立视觉手册，没有覆盖运行库中的既有手册，也没有调用付费模型。P0 的整条 Agent 浏览器业务流程与真实模型内容效果仍未验收，不能把组件测试和模拟供应商请求测试视为全流程完成。P1 的同造型跨场复用策略、场景日夜状态、断线后的阶段恢复和局部分镜重试仍待实现；当前 Agent `retry` 仍是重新提交请求，不能称为断点续跑。P2 后续仍需补同条件基线/新链路质量对比、穿越前后物件连续性和独立图片视觉验收。

## 结论

保留 Rust 后端、Vben 前端和现有业务数据，以 Toonflow-app 的 Agent 分工、专业内容模板、按需衍生与工作台交互作为功能基线。当前已具备两套 Agent 和主要执行阶段，重点是修通上下文与技能传递、统一相互冲突的规则、补齐执行过程反馈，而非重新实现一套 Agent。

先完成主剧场对齐，再增加专题剧场入口。古今混合应先在现有项目生效，新入口复用项目、角色身份、资产、分镜、轨道与任务服务。不要要求用户重建项目或重复填写简介。

## Agent 工作方式对比

| 维度 | Toonflow-app 基线 | Rust 当前实现 | 处理建议 |
| --- | --- | --- | --- |
| 剧本 Agent | 决策 → 骨架/改编/剧本执行 → 编辑审核 | 对应子 Agent、独立模型键、工具循环、保存接口均存在 | 保留结构；逐项核验实际产出与保存结果，不能按接口同名判定已对齐 |
| 生产 Agent | 决策 → 导演规划、衍生分析、衍生生成、分镜表、面板、分镜图 → 监制 | 阶段均存在，另有视频提示词及编辑工具；部分工具直接暴露给决策层 | 对齐职责边界；决策层委派任务，执行层持有本阶段写工具，监督层只读 |
| 动态技能 | 按选定画风和导演手册扫描目录，执行层获得技能目录及 `activate_skill` | `dynamic_skills` 只判断画风/手册是否非空，再放行整个 `art_skills/`、`story_skills/`；执行层未复用完整技能目录注入 | **P0**：按选定路径精确筛选，统一技能目录、加载接口与执行层上下文 |
| 技能内容存储 | 技法 Markdown 与运行时加载目录一致 | 同步脚本把画风/导演技法存进 `creative_manuals`，动态加载查询 `skill_list`；现有同步脚本没有把这类目录完整注册为动态技能 | **P0**：提供统一解析器，将手册条目映射为可加载技能；读取运行库确认实际缺口 |
| 项目上下文 | ScriptAgent 明确携带简介；生产执行层注入画风/叙事技能和模型信息 | 剧本父/子 Agent 与生产子 Agent带简介；生产决策层读取简介和画风却没有输出到生产上下文；资产提取输入也未显式带简介 | **P0**：统一上下文构建，所有阶段共享项目世界观、当前集/场、模型能力及引用资产 |
| 实时工作区 | 多个生产工具通过 Socket 向前端请求数据/执行写入 | 服务端从业务表读取剧本和资产，工作区保存规划内容 | 保留服务端数据来源；对齐前端实时刷新，避免依赖页面常驻才能运行 |
| 执行过程展示 | 子 Agent 独立消息，逐块消费流式输出，工具有过程消息 | 决策层有流式输出；剧本子 Agent 最后整体回填；生产执行循环使用非流式调用，内部请求 `emitter: None` | **P1**：贯通父子运行事件，展示当前阶段、工具结果、产出与失败位置，无需展示内部推理 |
| 记忆 | 近期消息、摘要、向量召回、深度检索；本地 ONNX | 同类能力已有，使用配置的 embedding 模型；普通相关记忆只搜索最近100条，失败可降级词项匹配 | 保留可配置模型；增加模型/维度版本、项目隔离和长历史召回验收，后续按需提供本地模型 |
| 失败恢复 | AbortSignal 与阶段重新派发 | 运行/工具记录、失败原因、重试关联已有；`retry` 重新提交原始请求；活跃 Agent 注册表仍在 Gateway 内存中 | 保留记录；补阶段检查点和幂等恢复。不能把当前重试称为断点续跑或宣称 Gateway 可横向扩展 |

主要证据：

- 上游 `src/agents/scriptAgent/index.ts`、`src/agents/productionAgent/index.ts`：决策与子 Agent、技能目录和流式执行。
- 上游 `src/agents/productionAgent/tools.ts`：Socket 工作区读写与生成工具。
- Rust `crates/modules/toon-server/src/toonflow_agents.rs:174`：项目上下文；`:448`：工具范围；`:601`：生产执行循环；`:1003`：决策层流式路径；`:1276`：重新提交式重试。
- Rust `crates/modules/toon-server/src/toonflow_agent_runtime.rs:203`：动态技能筛选；`:267` 附近：相关记忆检索。
- Rust `crates/modules/toon-server/src/toonflow_agent_tools.rs:377`：技能加载；`:1058`：各生产子 Agent 的工具范围及上下文。
- `script/sync-toonflow-content.mjs`：手册条目和独立 Skill 的不同导入路径。

## 内容实现对比

| 内容 | 已确认差异 | 应如何对齐 |
| --- | --- | --- |
| 事件 → 骨架 → 改编 → 剧本 | 三阶段与审核已实现。当前迁移明确把章节事件收敛为每章一条核心事件 | 保留已有每章一条规则；重要细节通过原文读取补充，为骨架/剧本记录来源章节，验收跨章因果、台词与集数覆盖 |
| 基础人物和场景造型 | 上游基础人物可带基础着装，只在明显换装/变身/形变时衍生。Rust 独立维护身份与 appearances，并要求分镜人物一律引用衍生 | 保留身份和造型分离的数据能力；改为可复用造型优先，仅必要变化创建新图。旧项目的中性底模不能直接作为正式服装入镜，必须核验已有成图是否符合本场造型 |
| 场景衍生 | 上游支持日/夜等时间变体；Rust `add_deriveAsset` 当前只允许人物，场景变化引导到分镜阶段 | 补同一空间的时间状态能力，复用现有场景一致性表/参考图；避免重复造两套场景关系。日夜状态与全新地点必须区分 |
| 资产提取 | 上游返回新资产和已有引用；Rust 增加 costume、年龄阶段和逐场 appearances，结构更丰富 | 保留新增结构；减少无必要的独立服装图片，完善已有人物/造型去重和场次绑定 |
| 润色与生图 | 上游接口查询 `type/intro`，但实际基础资产润色/生图 prompt 没用这两项；衍生批量生图会重新润色并更新 prompt | 不照搬遗漏与覆盖行为。当前未提交修复已把题材/简介传入资产润色和生图，并恢复衍生提示词编辑；后续验证最终发送文本尊重手改内容，完成入口一致性检查 |
| 分镜表 → 面板 | 上游按视频模式选择片段级纯文本或逐镜首帧，并使用画风/模型对应模板 | 对齐模板和字段语义；保留 Rust 的资产ID、参考顺序、姿态、时长、批次回滚检查。校验失败应定位具体行，不应无差别重写整集 |
| 监督审核 | 上游主要审核内容文档；Rust 分镜表写入后自动读取最新内容复审，并有机械校验 | 保留自动复审；将格式/引用错误和创作建议分开。审核绑定产出版本，内容改动后旧“通过”失效。文档审核不能冒充实际图片视觉验收 |
| 视频与成片 | 上游决策手册明确引导用户到视频面板生成/合成；Rust 已有视频提示词、轨道、导出及成片归档 | 对齐完整工作台操作；保留现有导出、归档和 Worker。Agent 自动生成视频属于另外的能力扩展，不能当作上游已有功能 |
| 古今混合 | 所查路径中没有专用穿越世界观机制；部分画风手册直接禁止另一时代物件 | 项目简介确定世界规则；场次确定时空；人物造型、道具保留各自年代；画风只负责审美。用户已明确的时代事实必须覆盖画风模板里的默认年代 |

## 不能原样复制的冲突

1. 上游生产决策 Skill 写“仅阶段4审核”，后文又要求阶段1和4审核；衍生分析在决策 Skill 中依赖导演预划，在执行 Skill 中却明确不读预划。先选定一致规则再接入。
2. Rust 初始内容同时留有旧生产顺序与“人物衍生优先”的覆盖段落；监督规则仍有“特殊光照走场景衍生”，而工具拒绝创建场景衍生。需要统一 Skill、工具和数据模型。
3. Rust 工具说明仍展示 `add_deriveAsset({assetsId,id,name,desc})`，实际处理要求 `appearanceId`。工具 schema、提示词示例与处理函数必须有同一份契约。
4. 上游读取了简介但没有把它用于基础资产 prompt；不能用“查询里有 intro”当作世界观已传递的证据。
5. 既有手册含带年代的禁止项，直接全量覆盖会重新引入古风与现代冲突。人工修改提示词、头像身份设定及用户自定义手册都应保留。

## 实施顺序与验收

### P0：主剧场的上下文、技能与规则一致性

1. 读取运行中的 Skill、手册、模型映射，与固定上游 commit 建立清单：来源、版本、项目适用范围、用户修改、当前消费者。
2. 实现共享上下文结构：项目题材/简介、选定美术与叙事手册、当前集/场、模型模式、资产ID和最新版本。接入资产提取、润色、生成、生产决策与执行阶段。
3. 修通技能目录与手册解析，按项目精确选取，执行 Agent 能实际加载对应内容；保留通用生产技法，避免加载其他画风的规则。
4. 统一各层工具 schema 与 Skill 调用示例；为创作内容设定明确优先顺序：用户当前明确修改 → 项目/剧本事实与已确认资产 → 选定画风技法 → 通用默认值。
5. 清理相互矛盾的阶段规则，阶段完成以实际保存的内容及必要媒体状态为依据。已有授权和项目配置直接使用，只询问真正缺少的创作选择。

验收：同一“古风画风 + 古今混合简介”的测试项目，现代404公寓保留电脑、冷白灯、冰雹、泡面桶；古代客栈保留古代结构；明确同框的古今元素并存；其他画风技能不出现在该项目执行上下文里。记录最终 prompt 可核查每项输入来源。

### P1：按需衍生、工作台反馈与恢复

1. 以“导演规划 → 按需衍生分析/生成 → 分镜表与审核 → 面板 → 生图”为默认逻辑，规划可先做，付费图片就绪只阻挡真正依赖它的媒体步骤；用户明确授权的步骤连续执行。
2. 同一人物、同一套造型跨场复用。仅变化时新增，允许现代/古装多个造型；已有身份底模、正式着装图与旧衍生关系继续兼容。
3. 支持同一场景日夜状态并保持空间参照；整合现有场景一致性能力。
4. 贯通子 Agent 进度和版本化产出；编辑保存后刷新工作区，重生成使用保存的人工 prompt。
5. 保存阶段输入版本、产出ID、生成任务ID与审核版本；重复点击/重试先查询产出和任务状态，避免再次付费生成已完成内容。

验收：同服装跨三场不重复生成；换古装时身份保持；只重试失败资产；断线重连能恢复进度；只改一镜不会覆盖其余镜头；人工 prompt 保存后实际发送内容可追踪。

### P2：内容质量回归与专题剧场

1. 用相同剧本、模型、尺寸和参考图分别跑基线与新链路，比较事实遗漏/新增、人物身份、服装、场景年代、分镜覆盖、调用次数与耗时。先用模拟模型验链路，再做限定范围真实供应商验证。
2. 增加“穿越剧场”入口或项目模板，复用主剧场；扩展时空设定、角色造型映射、穿越前后物件连续性。只有现有字段不足时才新增结构化字段。
3. 将文本审核与图片视觉验收区分展示，图片的身份/年代/物体正确性需要实际预览或独立视觉检查。

不按文件数量或同名API数量宣称“全面对齐”。完成标准是同一测试项目能走通：导入 → 事件 → 骨架 → 改编 → 剧本 → 资产/衍生 → 导演规划 → 分镜 → 视频 → 成片，且能编辑、取消、重试、刷新恢复。

## 数据和交付约束

- 先修 P0，再做 P1，专题剧场放在 P2；实施不批量覆盖数据库或改写旧作品。
- 若修改 Skill 基线或新增字段，追加编号迁移，不改已应用迁移；按 AGENTS.md 运行空库迁移测试、更新快照和迁移断言。
- 保留用户自定义 Skill/手册/提示词；升级需要内容版本或原始哈希匹配，避免无条件 upsert 覆盖。
- 保留单 Gateway 约束，媒体容量通过 Worker 扩展。
- 截至第二批没有运行真实 AI 对比、重新生成用户图片或部署尚未提交的修复。最新验证范围见“当前实施进度”；仅模拟调用与局部浏览器交互通过，不代表真实图片质量或整个制作流程已对齐。
